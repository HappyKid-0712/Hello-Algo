#include <bits/stdc++.h>
using namespace std;
vector<int> path;
int n;
vector<int> x, y;

void bfs()
{
}
int main()
{
  cin >> n;
  x.resize(n), y.resize(n);
  for (auto &it : x)
  {
    cin >> it;
  }
  for (auto &it : y)
  {
    cin >> it;
  }

  return 0;
}